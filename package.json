{
  "name": "Task-manager_Mern-app",
  "version": "1.0.0",
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "postinstall": "npm run bootstrap",
    "bootstrap": "lerna bootstrap --hoist",
    "start": "lerna run start",
    "test": "lerna run test",
    "diff": "lerna diff"
  }
}
